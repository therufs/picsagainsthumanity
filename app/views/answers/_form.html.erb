<%= form_for :question_answer, url: question_answers_path(id: @answer.id), method: :post do |f| %>

  <%= f.label :caption %>
  <%= f.text_field :caption %>

  <%= f.label "Pic to upload" %>
  <%= f.file_field :pic %>

  <%= f.submit %>

<% end %>
